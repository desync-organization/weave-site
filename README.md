# Weave

The social layer for open source: a single-page Next.js experience for discovering repositories through the people building them.

## Local development

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Android download

Place the release build at `public/weave.apk`. The homepage download buttons are already wired to that path.
